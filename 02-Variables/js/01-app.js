// Inicializando variable
var producto = "Monitor de 24 pulgadas";
console.log(producto);

// reasignando valor a la variable
producto = "Monitor de 19 pulgadas";
console.log(producto);

producto = 20
console.log(producto);

// Las Variables se pueden inicializar sin valor y asignarlo despues
var disponible

disponible = true;
disponible = false;

// Inicializando multiples varibles
var categoria = "Computadoras",
    marcador = "Nike",
    calificacion = 5;

// Reglas de las variables:

// Pueden tener: letras, numeros, _
// No pueden iniciar con numero
var 99dias;
var dias99;

var _01;
var 01_;

// Estilos para nombrar variables con más de una palabra

// más de una palabra.
var  nombreProducto = "Monitor"; // CamelCase
var nombre_producto = "Monitor"; //underscore
var NombreProducto = "Monitor"; // pascal case
var nombreproducto = "Monitor";